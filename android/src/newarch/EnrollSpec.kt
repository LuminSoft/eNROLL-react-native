package com.luminsoft.enroll.reactnative

import com.facebook.react.bridge.ReactApplicationContext

abstract class EnrollSpec internal constructor(context: ReactApplicationContext) :
    NativeEnrollSpec(context) {
}
